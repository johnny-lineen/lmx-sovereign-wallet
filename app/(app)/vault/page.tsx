import { SectionPlaceholder } from "@/components/placeholders/section-placeholder";

export default function VaultPage() {
  return (
    <SectionPlaceholder
      title="Vault"
      description="Secure storage for identity-linked artifacts."
      emptyMessage="Vault CRUD ships in a later phase."
    />
  );
}
